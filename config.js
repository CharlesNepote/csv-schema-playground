var config = {};

/**
 * http root of the app. Example:
 * config.port = "/";
 * config.port = "/rest";
**/
config.path = "/";

/**
 * TCP port. Example:
 * config.port = 8100;
**/
config.port = 8100;


module.exports = config;
