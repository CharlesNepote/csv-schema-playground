var config = {};

/**
 * http root of the app. Example:
 * config.path = "/";
 * config.path = "/rest";
**/
config.path = "/rest";

/**
 * TCP port. Example:
 * config.port = 8100;
**/
config.port = 8100;


module.exports = config;
